extends Node

signal player_died

signal debug_message(message: String)

signal camera_shake_requested(amount: float)

signal player_stats_changed(health: int, stamina: float)

signal button_pressed
signal portal_activated
